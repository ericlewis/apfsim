[project]
name = "pocket-sim"
version = "0.1.0"
description = "Deterministic Verilator-based APF contract simulator for Analogue Pocket core integration."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=8",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["apfsim/tests"]
